* This directory is created to store study materials if any.

* Any changes made to files in this directory should be brought in to notice to all
  team members immediately since all are referring to this common data while
  developing code.

* Team members are requested to execute "git fetch" and "git rebase" to update
  their local repository instead of "git pull" for this entire directory and
  sub-directory to avoid conflict.

* Team members can contribute to study materials directory.

FILE_READ_ACCESS=ALL;

FILE_WRITE_ACCESS=ALL;

CAUTION : SHARED DATA.
